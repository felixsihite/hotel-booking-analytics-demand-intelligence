# Analytics Warehouse

The SQLite warehouse is generated locally by the Python pipeline:

```text
data/warehouse/hotel_booking_analytics.sqlite
```

It is not committed to GitHub because it is a generated artifact. SQL query files remain versioned in `sql/`.
